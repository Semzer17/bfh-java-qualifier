package com.example.bfhqualifier.solver;

public interface SqlSolver {
    /**
     * Returns the final SQL query to be submitted.
     * Replace the returned string with your actual solution.
     */
    String buildFinalQuery();
}
