name: Build and Commit JAR

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Build (verbose)
        run: |
          mvn -B -DskipTests package
          echo "---- TARGET CONTENTS ----"
          ls -la target || true

      - name: Detect final JAR
        id: detect
        run: |
          JAR="$(ls target/*.jar | head -n1 || true)"
          echo "JAR=$JAR" >> $GITHUB_OUTPUT
          if [ -z "$JAR" ]; then
            echo "No JAR found in target/. Build likely failed."
            exit 1
          fi
          echo "Detected JAR: $JAR"

      - name: Prepare release files (both names)
        run: |
          mkdir -p release
          cp "${{ steps.detect.outputs.JAR }}" release/bfh-java-qualifier.jar
          cp "${{ steps.detect.outputs.JAR }}" release/bfh-java-qualifier-1.0.0.jar
          ls -la release

      - name: Commit JAR(s) to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin "$GITHUB_REF_NAME" || true
          git add release/bfh-java-qualifier.jar release/bfh-java-qualifier-1.0.0.jar
          git commit -m "CI: add compiled JAR(s) [skip ci]" || echo "No changes to commit"
          git push origin "$GITHUB_REF_NAME"

      - name: Upload JARs as artifact (fallback)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bfh-jars
          path: release/*.jar
